# @dshp-inx/token-stats bundle patch: Token usage statistics for DeepSeek Harness.
# Host half: sessionQuery-based aggregation + /ext/dshp-inx-token-stats/data route.
# Client half: Settings page section with overview/trend/heatmap/models tabs.

- insert:
    - id: dshp-inx-token-stats
      name: "@dshp-inx/token-stats"
